--// Dịch vụ cần thiết
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

--// Tạo ScreenGui chính
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "PVKA_CustomHub"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

--====================================================================--
-- 1. MÀN HÌNH INTRO (HIỆN CHỮ "PVKA" VÀ CHẤM TRÒN XOAY)
--====================================================================--
local introHolder = Instance.new("Frame")
introHolder.Name = "IntroHolder"
introHolder.Size = UDim2.new(1, 0, 1, 0)
introHolder.BackgroundTransparency = 1
introHolder.Parent = screenGui

local pvkaLabel = Instance.new("TextLabel")
pvkaLabel.Size = UDim2.new(0, 300, 0, 100)
pvkaLabel.AnchorPoint = Vector2.new(0.5, 0.5)
pvkaLabel.Position = UDim2.new(0.5, 0, 0.5, 0)
pvkaLabel.BackgroundTransparency = 1
pvkaLabel.Text = "PVKA"
pvkaLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
pvkaLabel.TextSize = 50
pvkaLabel.Font = Enum.Font.FredokaOne
pvkaLabel.TextTransparency = 1
pvkaLabel.Parent = introHolder

-- Tạo các chấm tròn loading xoay vòng quanh
local dots = {}
local numDots = 8
local radius = 45

for i = 1, numDots do
	local dot = Instance.new("Frame")
	dot.Size = UDim2.new(0, 10, 0, 10)
	dot.AnchorPoint = Vector2.new(0.5, 0.5)
	dot.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	dot.BackgroundTransparency = 1
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(1, 0)
	corner.Parent = dot
	
	dot.Parent = introHolder
	table.insert(dots, dot)
end

local rotConn
local angle = 0
rotConn = RunService.RenderStepped:Connect(function(dt)
	angle = angle + dt * 3
	for i, dot in ipairs(dots) do
		local theta = angle + (i * (math.pi * 2 / numDots))
		local x = math.cos(theta) * radius
		local y = math.sin(theta) * radius
		dot.Position = UDim2.new(0.5, x, 0.5, y)
	end
end)

TweenService:Create(pvkaLabel, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
for _, dot in ipairs(dots) do
	TweenService:Create(dot, TweenInfo.new(0.6), {BackgroundTransparency = 0}):Play()
end

task.wait(1.8)

TweenService:Create(pvkaLabel, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {TextTransparency = 1}):Play()
for _, dot in ipairs(dots) do
	TweenService:Create(dot, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
end
task.wait(0.5)

if rotConn then rotConn:Disconnect() end
introHolder:Destroy()

--====================================================================--
-- 2. GIAO DIỆN CHÍNH (MENU TO, HỆ THỐNG TAB, NÚT THU NHỎ, ĐÓNG)
--====================================================================--

-- Nút tròn mở/đóng menu ngoài màn hình
local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 50, 0, 50)
toggleButton.Position = UDim2.new(0, 20, 0, 20)
toggleButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
toggleButton.Text = "⚙️"
toggleButton.TextSize = 22
toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(1, 0)
toggleCorner.Parent = toggleButton

-- Khung Menu Chính (Kích thước 480x340)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 480, 0, 340)
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Position = UDim2.new(0.5, 0, 1.5, 0) -- Ẩn ở dưới lúc đầu
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.Visible = false
mainFrame.Parent = screenGui

local menuCorner = Instance.new("UICorner")
menuCorner.CornerRadius = UDim.new(0, 16)
menuCorner.Parent = mainFrame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(60, 60, 60)
stroke.Thickness = 2
stroke.Parent = mainFrame

-- Tiêu đề Menu
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -90, 0, 50)
titleLabel.Position = UDim2.new(0, 20, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "⚡ PVKA HUB ⚡"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 18
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = mainFrame

-- Nút Đóng (✕)
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -40, 0, 10)
closeButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextSize = 14
closeButton.Font = Enum.Font.GothamBold
closeButton.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 8)
closeCorner.Parent = closeButton

-- Nút Thu Nhỏ (-) / Phóng to (+)
local minimizeButton = Instance.new("TextButton")
minimizeButton.Size = UDim2.new(0, 30, 0, 30)
minimizeButton.Position = UDim2.new(1, -75, 0, 10)
minimizeButton.BackgroundColor3 = Color3.fromRGB(200, 150, 40)
minimizeButton.Text = "-"
minimizeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeButton.TextSize = 18
minimizeButton.Font = Enum.Font.GothamBold
minimizeButton.Parent = mainFrame

local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(0, 8)
minimizeCorner.Parent = minimizeButton

-- Thanh chứa Tab (Tab Bar)
local tabBar = Instance.new("ScrollingFrame")
tabBar.Size = UDim2.new(1, -30, 0, 40)
tabBar.Position = UDim2.new(0, 15, 0, 55)
tabBar.BackgroundTransparency = 1
tabBar.CanvasSize = UDim2.new(0, 300, 0, 0)
tabBar.ScrollBarThickness = 0
tabBar.Parent = mainFrame

local tabLayout = Instance.new("UIListLayout")
tabLayout.FillDirection = Enum.FillDirection.Horizontal
tabLayout.Padding = UDim.new(0, 10)
tabLayout.Parent = tabBar

-- Khung chứa Nội dung các Tab
local contentContainer = Instance.new("Frame")
contentContainer.Size = UDim2.new(1, -30, 1, -110)
contentContainer.Position = UDim2.new(0, 15, 0, 105)
contentContainer.BackgroundTransparency = 1
contentContainer.Parent = mainFrame

--====================================================================--
-- 3. HỆ THỐNG TẠO TAB (GẮN CÁC TÍNH NĂNG TÙY Ý)
--====================================================================--
local tabs = {}
local activeTab = nil

local function createTab(name)
	local tabBtn = Instance.new("TextButton")
	tabBtn.Size = UDim2.new(0, 120, 0, 35)
	tabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
	tabBtn.Text = name
	tabBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
	tabBtn.TextSize = 14
	tabBtn.Font = Enum.Font.GothamBold
	tabBtn.Parent = tabBar
	
	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 8)
	btnCorner.Parent = tabBtn
	
	local page = Instance.new("ScrollingFrame")
	page.Size = UDim2.new(1, 0, 1, 0)
	page.BackgroundTransparency = 1
	page.Visible = false
	page.CanvasSize = UDim2.new(0, 0, 0, 0)
	page.ScrollBarThickness = 4
	page.Parent = contentContainer
	
	local pageLayout = Instance.new("UIListLayout")
	pageLayout.Padding = UDim.new(0, 10)
	pageLayout.Parent = page
	
	tabBtn.MouseButton1Click:Connect(function()
		for _, t in pairs(tabs) do
			t.Btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
			t.Btn.TextColor3 = Color3.fromRGB(180, 180, 180)
			t.Page.Visible = false
		end
		tabBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
		tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
		page.Visible = true
	end)
	
	if not activeTab then
		tabBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
		tabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
		page.Visible = true
		activeTab = tabBtn
	end
	
	table.insert(tabs, {Btn = tabBtn, Page = page})
	return page -- Trả về trang (Page) để bạn tự chèn nút/chức năng vào đây
end

-- Ví dụ tạo sẵn 2 Tab mẫu (Bạn có thể đổi tên hoặc thêm bớt tùy ý)
local tab1 = createTab("🏠 Trang Chủ")
local tab2 = createTab("⚙️ Cài Đặt")

-- Ví dụ chèn chữ hướng dẫn vào Tab 1
local sampleLabel = Instance.new("TextLabel")
sampleLabel.Size = UDim2.new(1, 0, 0, 50)
sampleLabel.BackgroundTransparency = 1
sampleLabel.Text = "Khu vực trống. Bạn có thể tự code gắn các nút chức năng vào đây!"
sampleLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
sampleLabel.TextSize = 14
sampleLabel.Font = Enum.Font.Gotham
sampleLabel.TextWrapped = true
sampleLabel.Parent = tab1

--====================================================================--
-- 4. KÉO THẢ & HIỆN / ĐÓNG / THU NHỎ MENU
--====================================================================--
local dragging, dragInput, dragStart, startPos

titleLabel.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = mainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragging = false end
		end)
	end
end)

titleLabel.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart
		mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

local isMenuOpen = false
local isMinimized = false
local fullSize = UDim2.new(0, 480, 0, 340)
local minSize = UDim2.new(0, 480, 0, 50)

local function openMenu()
	if isMenuOpen then return end
	isMenuOpen = true
	isMinimized = false
	minimizeButton.Text = "-"
	mainFrame.Visible = true
	mainFrame.Size = fullSize
	mainFrame.Position = UDim2.new(0.5, 0, 1.5, 0)
	TweenService:Create(mainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
		Position = UDim2.new(0.5, 0, 0.5, 0)
	}):Play()
end

local function closeMenu()
	if not isMenuOpen then return end
	isMenuOpen = false
	TweenService:Create(mainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
		Position = UDim2.new(0.5, 0, -0.5, 0)
	}):Play()
	task.wait(0.25)
	if not isMenuOpen then mainFrame.Visible = false end
end

local function toggleMinimize()
	if not isMenuOpen then return end
	if isMinimized then
		isMinimized = false
		minimizeButton.Text = "-"
		TweenService:Create(mainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = fullSize}):Play()
	else
		isMinimized = true
		minimizeButton.Text = "+"
		TweenService:Create(mainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = minSize}):Play()
	end
end

toggleButton.MouseButton1Click:Connect(function()
	if isMenuOpen then closeMenu() else openMenu() end
end)
closeButton.MouseButton1Click:Connect(closeMenu)
minimizeButton.MouseButton1Click:Connect(toggleMinimize)

-- Tự động bật menu sau màn hình Intro PVKA
task.delay(2.3, openMenu)
